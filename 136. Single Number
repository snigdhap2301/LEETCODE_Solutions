class Solution {
    public int singleNumber(int[] nums) {
        int countNumber = 0;
        for (int i = 0; i < nums.length; i++) {
            countNumber ^= nums[i];
        }
        return countNumber;
    }
}
