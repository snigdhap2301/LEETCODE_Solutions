class Solution:
    def addBinary(self, a: str, b: str) -> str:
        a= int(a, 2)
        b = int(b, 2)
    
    # Add the integers
        sum = a + b
        sum_binary = bin(sum)[2:]
    
        return sum_binary
